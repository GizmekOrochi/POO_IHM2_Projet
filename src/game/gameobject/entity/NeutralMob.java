package game.gameobject.entity;

import game.gameobject.entity.Entity;

public class NeutralMob extends Entity {
    public NeutralMob(String name, int health, int moveSpeed) {
        super(name, health, moveSpeed);
    }
}

