package game.gameobject.environment;

// Wall Environment: Nothing can pass through this object
public class WallEnvironment extends Environment {

    // Constructor
    public WallEnvironment(String name, int health) {
        super(name, health);
    }
}
